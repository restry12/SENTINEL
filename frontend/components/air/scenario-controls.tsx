"use client"

// ScenarioControls is no longer used — scenarios have been replaced by live Socket.io data.
// This file is kept to avoid breaking any remaining imports but the component is a no-op.

export function ScenarioControls(_props: Record<string, unknown>) {
  return null
}
